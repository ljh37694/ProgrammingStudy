<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Document</title>
        <link rel="stylesheet" href="/style.css" />
    </head>
    <body>
        <%- include("nav.ejs") %>

        <div class="chatting-room-container">
            <% chatList.forEach((item) => { %>
                <div class="chatting-room">
                    <p><%= item.members[0] == user._id ? item.members_name[1] : item.members_name[0] %></p>
                    <a href="/chat/<%=item._id%>">채팅하기</a>
                </div>
            <% }) %>
        </div>
    </body>
</html>
